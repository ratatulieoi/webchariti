mixin contactInfo(contactData)
  .contact-sec-icon
    each contact in contactData
      .fo-solid(class=contact.iconClass)
        img(src=contact.imgSrc, alt=contact.imgAlt)
        h1= contact.title
        p= contact.description
        p= contact.contactNumber